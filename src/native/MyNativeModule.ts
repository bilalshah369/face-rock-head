import {NativeModules} from 'react-native';

/**
 * RNScanUtil
 * ↳ maps to ScannerModule.java
 * ↳ getName() returns "RNScanUtil"
 */
const {RNScanUtil} = NativeModules;

export default RNScanUtil;
